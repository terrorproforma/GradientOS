# Temporary IgH config for binding the master to the RevPi uplink / alternate NIC.
#
# Uses the NIC MAC so it stays valid even after interface renaming
# (current live host uses eth0 as the normal wired uplink).
MASTER0_DEVICE="c8:3e:a7:14:1c:75"

DEVICE_MODULES="generic"

