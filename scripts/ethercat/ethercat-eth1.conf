# Temporary IgH config for binding the master to the currently validated EtherCAT NIC.
#
# Uses the NIC MAC so it stays valid even after interface renaming
# (current live host reaches the slave chain via eth1 -> ethercat0).
MASTER0_DEVICE="c8:3e:a7:14:1c:76"

DEVICE_MODULES="generic"

